// Split into focused modules under ./route-scope/ (form state, route-option
// loading with its module-level cache, availability preview). This entry point
// keeps the public import path and component name stable.
export { RouteScopeForm as RouteScopeControls } from "./route-scope/route-scope-form";
